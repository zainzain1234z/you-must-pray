<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/green_900">

    <TextView
        android:id="@+id/matTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Prayer Mat Verification"
        android:textColor="@color/cream"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="40dp" />

    <TextView
        android:id="@+id/matInstructions"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Place your prayer mat in view and capture a photo."
        android:textColor="@color/cream"
        android:textSize="15sp"
        android:textAlignment="center"
        app:layout_constraintTop_toBottomOf="@id/matTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginHorizontal="32dp" />

    <ImageView
        android:id="@+id/matPreview"
        android:layout_width="220dp"
        android:layout_height="220dp"
        android:contentDescription="Prayer mat preview"
        android:scaleType="centerCrop"
        android:background="@color/green_300"
        app:layout_constraintTop_toBottomOf="@id/matInstructions"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp" />

    <Button
        android:id="@+id/captureButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Capture Photo"
        android:backgroundTint="@color/green_600"
        android:textColor="@color/cream"
        app:layout_constraintTop_toBottomOf="@id/matPreview"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"
        android:layout_marginHorizontal="48dp" />
</androidx.constraintlayout.widget.ConstraintLayout>
